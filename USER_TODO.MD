# USER_TODO

- **GitHub credentials**: The agent cannot push changes because the Git remote requires authentication. Please configure a Personal Access Token (PAT) with `repo` scope and store it in the repository's remote URL or a credential helper. Once the token is set, the agent can push commits.

- **Pending items to implement (IDEAS.md)**:
  - Provider Profile (already marked completed, but UI/display may need work)
  - Provider Schedule Management (table added, but UI integration pending)
  - Multi‑timezone support (provider schedules in location timezone, automated conversions)
  - Booking Policies: Cancellation Rules (marked completed), Deposit System, No‑show consequences
  - Notifications & Reminders: Appointment Reminders, Confirmation Messages, SMS Templates, Email Templates
  - Roles & Permissions: Admin, Location Manager, Provider, Front Desk/Receptionist
  - Integrations: Google Calendar, QuickBooks/Xero, Mailchimp/Klaviyo, Square/Clover
  - Reporting & Analytics: Dashboard Widgets, Provider Performance, Customer Insights, Location Comparison
  - Sample data for new tables (provider_schedules, etc.)
  - Verify the web app runs without errors, fix any CSS/rendering issues, generate QR code link, and send to Telegram.

- **Next steps after credentials are set**:
  - Implement each pending item, commit, push, update IDEAS.md, verify app, and report.
